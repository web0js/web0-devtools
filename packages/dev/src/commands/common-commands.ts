import { createCommand } from './create-command'

export const lerna = createCommand('lerna')

export const tsc = createCommand('tsc')

export const rimraf = createCommand('rimraf')

export const prettier = createCommand('prettier')

export const tslint = createCommand('tslint')

export const eslint = createCommand('eslint')
